# Empty uses the Vite development proxy or the configured Vercel proxy.
VITE_API_URL=
# For direct API deployment instead: https://your-backend.onrender.com
# See README for cookie and OAuth callback configuration.
